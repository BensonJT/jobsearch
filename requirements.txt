# Core — ATS-direct sweep + DuckDB job store (single file, no server process)
python-dotenv
httpx
duckdb
beautifulsoup4
lxml

# Testing
pytest

# Finder (optional): scikit-learn numpy scipy joblib
# Embeddings (optional): sentence-transformers torch  (or: fastembed)
